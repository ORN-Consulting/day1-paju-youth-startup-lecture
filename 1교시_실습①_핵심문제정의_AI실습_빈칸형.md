# [1교시 실습①] 핵심문제 정의 — AI에게 직접 물어보기

> **이 실습의 목적**
> 강의 내용을 듣기만 하는 게 아니라, **AI에게 "어떻게 물어야 하는가"를 직접 만들어 보는 것**입니다.
> 아래 프롬프트에는 `[ ___ ]` 빈칸이 있습니다. **이 빈칸을 채워야 프롬프트가 작동합니다.**
> 빈칸이 안 채워진다면 — 그건 오늘 배운 개념(핵심문제 4조건, 잘못된 4패턴 등)을 아직 내 것으로 만들지 못했다는 신호입니다. 강의 슬라이드를 다시 보고 채워 보세요.

---

## 사용 방법

1. **ChatGPT** 또는 **Claude** (무료 버전 가능)에서 **새 대화 1개**를 엽니다.
2. **STEP 0 → STEP 6 순서대로**, 한 대화 안에서 이어서 입력합니다. (대화를 새로 열면 AI가 앞 내용을 잊습니다.)
3. `[ ___ ]` 부분을 **본인이 직접 채워** 입력합니다.
4. AI는 **정답 문장을 대신 써주지 않습니다.** 무엇이 문제인지 진단하고, 어디를 보강해야 하는지만 알려줍니다. → 다시 쓰는 건 본인 몫입니다.

---

## STEP 0 — AI를 '문제정의 코치'로 세팅 (대화 시작 시 1번만)

> 목표: 이후 모든 단계에서 AI가 "정답을 주는 도우미"가 아니라 "진단하고 보강점을 짚는 코치"로 행동하게 만든다.

```
너는 지금부터 청년창업 강의의 '문제정의 코치'다. 아래 규칙을 대화가 끝날 때까지 지켜라.

[규칙]
1. 내 문제 진술을 절대 대신 고쳐 쓰지 마라. 모범답안·완성 예시도 주지 마라.
2. 너의 역할은 두 가지뿐이다 ── (1) 무엇이 문제인지 진단한다 (2) 어디를 보강해야 하는지 짚는다.
   그 다음 다시 쓰는 것은 내가 한다.
3. 진단 기준은 다음 두 가지만 사용하라.
   - 핵심문제 4조건 : [ ① 강의에서 배운 핵심문제의 4가지 조건을 여기에 적기 ]
   - 잘못된 4패턴   : [ ② 강의에서 배운 잘못된 문제정의 4가지 패턴을 여기에 적기 ]
4. 매번 답할 때 칭찬은 한 줄 이내로 줄이고, 곧바로 '다시 생각할 지점'을 짚어라.

이해했으면 "준비됐습니다. STEP 1 진술을 붙여넣으세요"라고만 답하라.
```

**채우기 힌트**
- `[ ① ]` → 슬라이드 "핵심문제의 4가지 조건" (특정 인구·맥락 / 관찰·측정 / 시급·중요 / …)
- `[ ② ]` → 슬라이드 "잘못된 문제 정의 4가지 패턴"
- ※ 이 두 빈칸을 채우는 과정 자체가 복습입니다. 외워서 적어 보세요.

---

## STEP 1 — 내 한 줄 진술 진단하기 ★깊게 고민

> 목표: 내가 쓴 진술을 **4패턴·4조건에 하나씩 비춰** 본다. 내가 쓴 문장이 어디서 걸리는지를 항목별로 직접 확인하며, 1교시 개념을 다시 익힌다.

```
[STEP 0의 코치 규칙대로 진단해줘]

내 한 줄 문제 진술:
『 [ ③ 본인이 정의한 핵심문제를 한 줄로 ] 』

다음을 하나씩 짚어가며 진단해줘 (정답 문장은 쓰지 말 것):
1. STEP 0에서 내가 알려준 '잘못된 4패턴'을 하나씩 적용해, 이 진술이 각 패턴에 해당하는지 패턴별로 판정해줘.
2. STEP 0에서 내가 알려준 '핵심문제 4조건'을 하나씩 적용해, 각 조건을 충족하는지 조건별로 판정해줘.
3. 진술 안에 '원인'이나 '솔루션'이 숨어 들어가 있는지 짚어줘.
4. 마지막에, 내가 다시 쓸 때 보강해야 할 지점만 항목별로 알려줘.
```

**채우기 힌트**
- 이 STEP에서 본인이 채울 빈칸은 `[ ③ ]` 진술 하나뿐입니다. 4패턴·4조건은 STEP 0에서 이미 적었으므로, AI가 그것을 **하나씩** 적용해 판정합니다.
- AI 출력을 보며 "내 진술이 패턴1에 걸리네 / 조건3이 비었네"를 직접 확인하세요. → 4패턴·4조건을 다시 익히는 과정입니다.

> **AI 진단을 받은 뒤** → 진술을 직접 다시 씁니다. 만족스러울 때까지 STEP 1을 반복하세요. (다시 쓴 진술을 또 붙여넣으면 됩니다.)

---

## STEP 2 — 누가·언제·어디서 (페르소나와 맥락) 점검 ○가볍게

> 목표: 진술이 '모든 사람, 모든 곳'이 아니라 **특정 인구·맥락**을 담고 있는지 확인한다.

```
내가 정한 핵심문제의 대상과 맥락이다.
- 누가(구체적 페르소나): [ ④ 본인 입력 ]
- 언제·어디서(상황·맥락): [ ⑤ 본인 입력 ]

이 두 가지가 [ ⑥ AI에게 점검시킬 기준 — 충분히 구체적인지 / 관찰·측정이 가능한지 ] 진단해줘.
부족하면 어떤 정보를 더 좁혀야 하는지만 알려줘. 예시 답은 쓰지 마.
```

**채우기 힌트**
- `[ ⑥ ]` → "구체적인가", "관찰·측정 가능한가"를 봐달라고 지시하면 됩니다.

---

## STEP 3 — 왜? 근본원인까지 파고들기 (5 Whys) ★깊게 고민

> 목표: 표면 답에서 멈추지 말고 **통제 가능한 근본원인**까지 내려간다. 단, 우리가 손댈 수 없는 영역에 닿으면 멈추고 '가정'으로 분리한다.

```
내 핵심문제에 대해 '왜?'를 단계적으로 파고들려 한다.

내 핵심문제: 『 STEP 1에서 다시 쓴 진술 』
지금까지 내가 생각한 원인 사슬:
- 왜? 1단계: [ ⑦ 본인 입력 ]
- 왜? 2단계: [ ⑧ 본인 입력 ]
- (더 있으면 계속)

다음을 코치해줘 (답을 대신 내려주지는 말 것):
1. [ ⑨ AI에게 시킬 것 — 내 원인 사슬이 표면에서 멈췄는지, 더 파고들 단계가 남았는지 판정 ]
2. [ ⑩ AI에게 시킬 것 — 어느 지점이 '우리가 통제할 수 없는 영역'에 닿았는지, 그래서 가정(Assumption)으로 분리해야 하는지 ]
3. 한 번에 한 단계씩만 되물어줘. 내가 답하면 그 다음 '왜?'를 던지는 방식으로 진행하자.
```

**채우기 힌트**
- `[ ⑨ ]` → "표면에서 멈췄는지, 2~3단계 더 파야 하는지 봐줘".
- `[ ⑩ ]` → 슬라이드 21p 기억하세요. '한국 경제 구조의 문제' 같은 데까지 가면 **우리가 다룰 수 없는 영역** → 멈추고 가정으로 분리. 이걸 AI가 짚게 만드는 지시입니다.
- 5 Whys의 끝은 **'우리가 다룰 수 있는' 곳**에서 끝나야 합니다.

---

## STEP 4 — 증거(E)와 가정(A) 구분 ○가볍게

> 목표: 트리의 각 항목이 **검증된 사실(E)**인지 **아직 추측(A)**인지 태그를 붙인다. (A)로 분류된 것이 곧 다음 PSF 인터뷰에서 확인할 질문이 된다.

```
내 핵심문제와 원인들에 (E)증거 / (A)가정 태그를 붙이려 한다.
항목 목록:
- [ ⑪ 핵심문제·원인들을 한 줄씩 나열 ]

다음을 진단해줘:
1. 각 항목이 (E)인지 (A)인지 내가 판단할 수 있도록, [ ⑫ AI에게 시킬 것 — 무엇을 근거로 E/A를 나눠야 하는지 기준을 되물어 확인 ]
2. (A)로 분류될 만한 항목이 있으면, 그것이 왜 아직 가정인지 짚어줘.
3. (A) 항목들을 모아, 다음 PSF 인터뷰에서 확인할 질문 목록으로 정리해줘.
```

**채우기 힌트**
- `[ ⑫ ]` → "무엇이 검증된 사실이고 무엇이 추측인지, 그 판단 기준을 알려줘" 식으로 지시하면 됩니다.
- **(A) 태그가 붙은 항목 = 다음 PSF 인터뷰의 질문 목록**입니다. STEP 6 정리에서도 이 목록이 쓰입니다.

---

## STEP 5 — 우선순위 점수 매기기 (3축) ○가볍게

> 목표: 핵심문제가 여러 개일 때, **무엇을 BM의 출발점으로 삼을지** 가중 점수로 정한다.

```
내 핵심문제 후보들에 우선순위 점수를 매기려 한다.
가중치는 [ ⑬ 강의에서 배운 3축의 가중치 — 긴급도 __% / 중요도 __% / 해결가능성 __% ] 이다.

핵심문제 후보:
- 후보 1: [ 본인 입력 ]   (긴급 __/5, 중요 __/5, 해결가능 __/5)
- 후보 2: [ 본인 입력 ]   (긴급 __/5, 중요 __/5, 해결가능 __/5)

다음을 코치해줘:
1. [ ⑭ AI에게 시킬 것 — 내가 매긴 점수의 근거가 타당한지, 점수만 높고 근거가 약한 항목은 없는지 ]
2. 가중치를 적용한 총점 계산식만 보여주고, 결과 해석은 내가 직접 하게 해줘.
```

**채우기 힌트**
- `[ ⑬ ]` → 슬라이드 23p. 긴급도 **30%** / 중요도 **40%** / 해결가능성 **30%**. (총점 = 긴급×0.3 + 중요×0.4 + 해결가능×0.3)
- 상위 1~3개가 **BM(비즈니스 모델)의 출발점**이 됩니다.

---

## STEP 6 — 오늘 실습 전체를 기록으로 정리하기 (대화 마지막에)

> 목표: 그동안 묻고·다시 쓰고·진단받은 내용이 흩어져 있다. 이 대화 전체를 거슬러 올라가 **STEP별 기록 한 장**으로 정리한다. AI가 새로 지어내지 않고, 실제로 오간 것만 모으게 한다.

```
이번 대화 전체를 처음부터 끝까지 되짚어서, 내가 STEP 1~5에서 한 작업을
하나의 실습 기록으로 정리해줘. 다음 규칙을 지켜라.

[규칙]
1. 이 대화에서 실제로 내가 입력했거나 네가 진단한 내용만 사용하라.
   대화에 없는 내용을 새로 지어내거나 보강해서 채우지 마라. 없으면 "(미작성)"으로 표시.
2. 아래 형식 그대로, 마크다운 코드블록 하나에 담아 출력하라. (내가 복사해서 .md 파일로 저장할 것이다)
3. 내 최종 진술/원인/점수는 '처음 버전'이 아니라 '내가 마지막으로 고친 버전'을 기준으로 정리하라.

[정리 형식]
# 1교시 실습 기록 — 핵심문제 정의

## STEP 1. 핵심문제 진술
- 처음 진술 →
- 최종 진술 →
- AI가 짚은 보강점(4패턴·4조건) →
- 내가 고친 이유 →

## STEP 2. 누가·맥락
- 페르소나 / 상황 →

## STEP 3. 5 Whys 근본원인
- 왜? 사슬 (1→2→3단계) →
- 통제 불가로 가정 분리한 지점 →

## STEP 4. 증거(E)·가정(A)
- (E) 항목 →
- (A) 항목 = 다음 PSF 인터뷰 질문 →

## STEP 5. 우선순위
- 후보별 점수와 총점 →
- 1순위로 고른 핵심문제 →

## 오늘 내가 가장 약했던 부분
- (빈칸 채우기가 막혔거나 AI가 반복해서 지적한 STEP) →
```

**사용 안내**
- 채울 빈칸이 없습니다. 그대로 복사해 마지막에 입력하면 됩니다.
- AI가 출력한 코드블록을 복사 → 메모장이나 문서에 붙여넣고 `.md`로 저장하면 본인 실습 기록이 됩니다.
- `(미작성)`이 많은 STEP = 오늘 끝까지 못 간 곳입니다. 복습 우선순위로 삼으세요.

---

## 이 실습이 끝나면 손에 남는 것

- 다시 쓴 **핵심문제 한 줄 진술** (현상이 아닌, 4조건을 갖춘)
- **5 Whys로 내려간 근본원인** + 통제 불가 영역에서 분리한 가정
- **(A) 태그 목록** = 다음 PSF 인터뷰에서 물어볼 질문들
- **우선순위 점수** = 어떤 문제부터 BM으로 풀지

> 빈칸이 잘 안 채워졌던 STEP이 있다면, 그 부분이 오늘 가장 약한 개념입니다. 강의 슬라이드의 해당 페이지를 다시 보세요.

---

*기준 출처: 본 강의안 「비즈니스 모델 수립 및 점검」(이동호 / ㈜오른경영컨설팅) Day 1 1교시 — 핵심문제 4조건(p16), 잘못된 4패턴(p18), 5 Whys(p21), 증거·가정 태그(p22), 우선순위 3축(p23)*
